# Git LFS tracking for large SPA assets
spa-assets/**/*.json filter=lfs diff=lfs merge=lfs -text
spa-assets/**/*.js filter=lfs diff=lfs merge=lfs -text

# Exclude small metadata files from LFS
spa-assets/metadata/*.json !filter !diff !merge text

# Text files should have normalized line endings
*.md text eol=lf
*.json text eol=lf
*.js text eol=lf
*.ts text eol=lf

# Archive formats
*.har binary
*.gz binary
*.zip binary
